package com.engine.graphics;

import java.awt.Graphics;

/**
 * <h1>DebugScreen</h1>
 * A debug screen is where the information for
 * a project's debug mode is collected and rendered.
 * The showDebug() method is ran every render tick
 * while debugShown is true in Core.
 * 
 * @author Ryan Hochmuth
 */
public class DebugScreen
{
	/**
	 * Draw the contents of this DebugScreen
	 * to the screen.
	 * @param g
	 */
	public void showDebug(Graphics g)
	{
		
	}
}